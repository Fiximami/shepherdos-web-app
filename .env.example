# ShepherdOS web app — staging / local API configuration
NEXT_PUBLIC_API_BASE_URL=https://shepherdos-api.onrender.com

# Optional default church slug used to prefill sign-in (users can override in the form)
NEXT_PUBLIC_CHURCH_SLUG=
